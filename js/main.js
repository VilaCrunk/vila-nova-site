/* =============================================================
   VILA NOVA — JavaScript principal
   -------------------------------------------------------------
   Interações (menu mobile, header no scroll, animações de
   reveal, etc.) serão adicionadas conforme cada seção pedir.
   Sem lógica definida ainda.
   ============================================================= */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
  // Interações entram aqui, seção por seção.
});
